<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-Whatsapp</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h3>All chats !</h3>
    <form method="get" action="/chats/new">
        <button>New Chat</button>
    </form><br>
    <% for(let chat of chats) { %>
        <div class="chat">
            <p>From : <i> <%=chat.from%> </i></p>
            <div class="msg">
                <p><%=chat.msg%></p>
                <div class="form">
                    <form method="get" action="/chats/<%=chat._id%>/edit">
                        <button class="button">edit</button>
                    </form>
                    <form method="post" action="/chats/<%=chat._id%>/delete?_method=DELETE">
                        <button class="button">Delete</button>
                    </form>
                </div>
            </div>
            <p>Received By : <i> <%=chat.to%> </i></p>
            <hr>
            <p><%=chat.created_at.toString().split(" ")[4]%></p>
            <p><%=chat.created_at.toString().split(" ").splice(0,4).join("-")%></p>
            <%if(chat.updated_at) {%>
                <div>
                    <p> Message updated at : <%=chat.updated_at.toString().split(" ").splice(0,4).join("-")%> on <%=chat.updated_at.toString().split(" ")[4]%></p>
                </div>
                <%}%>
        </div><br><br>
    <%  }  %>
</body>
</html>